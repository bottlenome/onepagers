# CLAUDE.md

このリポジトリでの開発ルール。

## サブエージェント運用

- サブエージェントの並列起動は**最大 5 並列まで OK**（ユーザー承認済み）。
- 簡単・定型のタスク（例: iut-lean-verification の定型モジュール実装）は積極的にサブエージェントへ委譲し、サブエージェントが行き詰まった場合は本体（親）が引き継ぐ。
- サブエージェントは新規ファイルのみ作成し、共有ファイル（IUT.lean・build.sh・dashboard.md・graph 系）の更新は親が統合時に一括で行う（競合回避）。

### モデル tier 配分ルール（コスト最適化・必守）

サブエージェントのモデルは**タスクの難度 tier に合わせて配分する**（§7.5 準拠）。**Fable を安易に使わない**——Fable は最も高価な tier-L 枠であり、乱用しない。

| tier | model | 任せるタスク |
|---|---|---|
| **S** | `sonnet` | capstone 束ね（新規証明ゼロ）・既存定理の再輸出/クローン・witness 演算だけの補題・ドキュメント同期 |
| **M** | `opus` | 代表レベル計算・確立イディオムの新インスタンス（rate-bound 適用・分離性翻訳・付値/塔の写経）・Phase II 実装（設計済み前提） |
| **L** | `fable` | **新イディオムの発明・Phase II/III の詳細化ラウンド・HELP スポット解決・margin 定数の設計のみ**。これ以外に Fable を割り当てない |

- 迷ったら 1 段安い方（M と L で迷えば M=opus）。tier L 項目を親が主導する場合も、実装本体は M/S に委譲し、Fable は詰まりの解決だけに限定する。
- 各ラウンドの起動時に、割り当てた tier と model を（ユーザー向け報告に）明示する。

### 5 並列を使い切る工夫（必守）

各統合ラウンドで**最大 5 並列をなるべく埋める**。埋め方の指針:

- 5 柱（A/B/C/D/E）の残件を横断で拾い、独立な新規ファイル単位に切って並列化する（依存のある段は直列、独立な切片は並列）。
- tier を混ぜる: 実装 opus 数本 + 詳細化 fable 1 本 + capstone/クローン sonnet 1 本、のように**安いモデルで枠を埋める**のがコスト効率的。
- Phase III の本丸（D-1・A-3 幾何入力）は直接実装せず、まず**詳細化ラウンド（fable 1 枠）**で段階分解してから実装枠（opus）を割り当てる。
- 依存で並列数が足りないときは、独立な tier-S 補題・capstone・ドキュメント同期で残り枠を埋める。

## 柱別進捗率の表示・更新ルール（必守）

- **各統合ラウンドの完了時**に、`iut-lean-verification/graph-meta.json` の `pillars.<X>.progress_pct` / `status` を成果に合わせて更新し、`dashboard.md` の証明率表と整合させる（過大主張しない・「正直な限定」が残る限りは 100 にしない）。
- 更新後は `cd iut-lean-verification && python3 tools/gen_graph.py` で `graph.json` を再生成しコミットする。
- ユーザーへの**ラウンド報告には柱別進捗率（core/A/B/C/D/E）を必ず表形式で載せる**。

## ダッシュボード更新ルール

プロジェクトのコードを変更した場合、関連するダッシュボードも必ず更新すること。

1. **プロジェクト個別の `dashboard.md`** — 機能追加・変更・バグ修正時に該当プロジェクトの `<project>/dashboard.md` を更新する
2. **ルートの `dashboard.md`** — プロジェクトの追加・削除・説明変更時にプロジェクト一覧テーブルを更新する
3. **ルートの `index.html`** — プロジェクトの追加・削除・説明変更時にカードの内容を更新する

### チェックポイント

- 新規プロジェクト追加時: `<project>/index.html`, `<project>/dashboard.md`, ルート `dashboard.md`, ルート `index.html` の4箇所を更新
- 既存プロジェクト変更時: `<project>/dashboard.md` の機能リストや説明が現状と一致しているか確認
- 説明文の変更時: ルート `dashboard.md` のテーブルとルート `index.html` のカードの両方を同期

### iut-lean-verification の証明グラフ

- 入口ページは `iut-lean-verification/index.html`（Cytoscape の依存グラフ）、詳細レポートは `report.html`。
- **IUT モジュール追加・依存変更時**は `cd iut-lean-verification && python3 tools/gen_graph.py` を再実行して `graph.json` を再生成しコミットする。
- 新規モジュールの柱（pillar）割当は `tools/gen_graph.py` の `PILLAR` 辞書に追記。文献PDFリンク・capstone/hub 注釈・柱の進捗率は `graph-meta.json` を手動更新する（柱進捗率は `dashboard.md` の証明率表に整合させる）。

## ゲーム開発の設計指針（櫻井政博メソッド）

ゲームの新規作成・改修時は `sakurai-methodology/index.html` に記載された設計原則を参照すること。
以下は特に重要な原則の要約。

### コア原則

1. **リスクとリターン** — ゲーム性＝かけひき＝「リスクを抑えてリターンを得る工夫」。全てのゲームメカニクスでリスクとリターンのバランスを意識する
2. **カービィズム（初心者ファースト）** — 誰でも遊べて上級者も極められる両立設計。操作は直感的に、スキルシーリングは高く
3. **プレイヤー視点** — 開発者の都合ではなくプレイヤーが何を感じるかから設計を始める
4. **プレゼンはスピード** — 無駄をそぎ落とし必要なことだけ伝える。UI・チュートリアル・体験全体に適用
5. **遊びの仕事は遊びじゃない** — プロとして需要に応える

### 設計チェックリスト

ゲーム機能を追加・変更する際に確認すべき項目:

- [ ] **リスクとリターン**: この機能にかけひきの要素があるか？ ハイリスク行動にはハイリターンが対応しているか？
- [ ] **ギュッとしてパッと解消**: 緊張と解放のサイクルが体験として成立しているか？
- [ ] **目の前に吊られたごほうび**: プレイヤーの目の前に常に次のゴールが見えているか？
- [ ] **最初にクライマックスを**: 序盤で最高の体験を提供してプレイヤーを掴めているか？
- [ ] **平均と平凡は同じ**: キャラクター・職業・装備に個性的な尖りがあるか？
- [ ] **情報の優先順位**: 今プレイヤーが必要としている情報だけを見せているか？
- [ ] **最短導線**: 目的の操作に到達するまでのステップ数は最小か？
