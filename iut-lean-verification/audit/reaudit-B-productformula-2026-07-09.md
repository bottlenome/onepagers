# 柱B 再監査（B5 積公式の実証後）2026-07-09

- 監査者: 独立エージェント1名（opus・敵対的・実 Lean 定義のみ・自己申告非共有）
- 契機: `IUT/B5ProductFormulaQ.lean`（ℚ 上の積公式 ∏_v|x|_v=1 を一般 x∈ℚˣ で実証）追加後の B 柱再評定
- 基準: `AUDIT_RUBRIC.md` / `target_ledger.json`

## 結果: **B5 0→0.5**（B 柱 complete_pct 10→18）

| id | 前回 | 今回 | 判定 |
|---|---|---|---|
| B1 | 0.5 | 0.5 | 据置（pvqVal は大域 ℚ の付値・局所体 K_v/分岐フィルトレーション/実 Galois 群に触れず） |
| B2 | 0 | 0 | 据置（相互写像・実 LCFT の本物定理は新規実装されず） |
| B3 | 0 | 0 | 据置（Artin 導手/different 寄与なし） |
| B4 | 0 | 0 | 据置（Hasse–Arf 寄与なし） |
| **B5** | **0** | **0.5** | **昇格**（積公式 ∏_v\|x\|_v=1 を実対象上・一般 x∈ℚˣ で実証） |
| B6 | 0 | 0 | 据置（Kummer 理論寄与なし） |

## 監査の核心判定（B5 昇格の論拠）

以前の B 監査は B5 を status 0 とし、その理由を明示していた:「\|·\|_∞ と v_p は積公式の材料だが、**積公式の定理そのもの（∏_v \|x\|_v=1）が Lean に存在しない（材料のみ）**」。今回その定理が実装された。監査者は DAG を主語ごとに実 Lean で確認:

- **(a) 主語は全て実対象**（模型/surrogate でない）:
  - `arpAbs`(=qAbs): 実 ℚ≥0 アルキメデス絶対値 \|num\|/den。
  - `pavAbs`: \|x\|_p = p^{−v_p(x)} を本物の PreRat（num=p^{(−v)⁺}, den=p^{v⁺}）で。乗法性 `pav_mul`・台外自明 `pav_trivial` は**証明済**。
  - `pvqVal`/`pvqNatVal`: fuel 有界再帰の実 p 進付値、加法性は `prime_pow_extract`+Euclid から**導出**。
  - `pfcFactors`/`pfc_prod_factors`/`pfc_vp_count`: 実・算術基本定理の積形（listProd=n・multiplicity=出現回数）を**証明**。
  - `fspProd`: 実 `ratRing.mul` 上の有限積。
- **(b) 非空虚**: `b5Support x = b5Dedup(pfcFactors\|num\| ++ pfcFactors den)` は num·den を割る素数全体。例 x=6/5 で台={2,3,5}（非空）、`pavAbs 2 x`=2^{−1}。特定数 toy でない。
- **(c) 一般性**: 一般 x∈ℚˣ（x.num≠0）で成立、固定値でない。
- **公理**: `#print axioms b5_product_formula` = `[propext, Quot.sound]` のみ（監査者自身が lake env lean で確認）。sorry/新規 Classical.choice なし。

→ 積公式側 ∏_v\|x\|_v=1 は**実 \|·\|_∞・実 \|·\|_p・実素因数分解の上で本物に証明**。忠実な狭い but 本物の大域的主張として **0→0.5**。

## 満点(1)にしない論拠（正直な限定）
「大域類体論」本体（大域相互写像・イデール類群の Artin 写像・一般数体）は未。積公式だけでは 0.5 が上限。

## プロセスの健全性
- これは撤回後**初の実 complete_pct 前進**。第140–147弾の「模型作業を昇格と偽って +1%」水増しとは異なり、**独立監査が『以前 0 とした具体的理由（定理の不在）を実定理が解消した』と確認した上での昇格**。自己申告ではない。
- 前回 §2(b) ラウンド（二次体・素点・μ_3）は本物でも横這いだった。同じ監査基準が、今回は**満点条件でなく "以前 0 の根拠" を解消した本物**に対して正しく 0.5 を与えた。過小評価も過大評価もしない двусторонний な評定。

## 次手（B5 を 1 に近づける / 他項目）
- B5→1: 大域相互写像・イデール類群の実 Artin 写像・一般数体の積公式。
- 積公式の材料（実 v_p・実 \|·\|_∞）は B1 の局所側にも接続しうるが、B1 満点は分岐群フィルトレーションを実 Galois 群上に要する（実 G_K の不在が律速）。
