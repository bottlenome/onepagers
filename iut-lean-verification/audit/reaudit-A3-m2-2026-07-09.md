# 独立再監査 A3 — 一般 n 円分塔 M2（res₁ 全射性＋∀n 全機構）

- 日付: 2026-07-10（起票ラウンド名 2026-07-09 の継続）
- 監査者: 独立・敵対的監査（opus・実 Lean 定義のみ・実装者の自己申告非共有）
- 対象柱: A（遠アーベル / 実 π₁^ét）・項目 A3（実絶対 Galois 群 G_K = 有限 Galois 群の逆極限）
- 前回: A3 = 0.6（`reaudit-A3-res39-2026-07-09.md`・2 段塔 ℚ(ζ₃)⊂ℚ(ζ₉)＋遷移射 res₁ の存在・compat・群準同型のみ・res₁ 全射性未証明・一般 n 未達）
- **判定: A3 0.6 → 0.65**（設計 §5 M2 予測と一致）。柱A% 38.0→38.6 で **38 → 39**。complete_pct 主指標が動いた。

---

## 0. 自走検証の証跡（自己申告に依らず監査者が実行）

### 0.1 フルビルド
```
export PATH="/root/lean4/bin:$PATH"; bash build.sh
→ EXIT=0
→ "OK: all theorems verified, no sorry."
```

### 0.2 #print axioms（新規 A3 対象・全て [propext, Quot.sound]）
build.sh 末尾の axiom 監査出力を目視確認。新規 A3 M2 対象は例外なく
`depends on axioms: [propext, Quot.sound]`:

```
ctsStretch_mul, ctsPhi_two_eq, cts_pow_sub_one, cts_cong
cg9_galois_order_six, cg9Subst_mul, cg9_aut_ext
est_transport
cr39_surjective, cs39_res_sigma2
eitPhi_irreducible, eitFreshman
cteField, cteMap_mul, cteIota
ctm_order, ctm_root_in_powers, ctmFind_spec
cae_decompose, cae_aut_ext
csaSub_mul, csaAut, csaAut_mem
ctr_compat, ctrResHom, ctr_surjective
```

`Classical.choice` に依存する対象はコード全体で既存の 2 件のみ
（`bi_implies_mono_classical`・`theta_labels`＝M1-2 の意図的 choice 使用点）で、
**今回の A3 M2 機構は新規 Classical.choice をゼロ混入**。特に監査で厳しく見た 3 箇所:
- `eitFreshman`（freshman's dream・witness B を ∃ で保持し ∃-除去のみ）→ choice-free ✔
- `ctmFind`/`ctr` の冪走査（`ctmFindGo`・有界 Nat 再帰）→ choice-free ✔
- `ctr_inv_exists`（Hensel 逆元持ち上げ・純 Nat 帰納・`ctr_tex` は明示 t を返す）→ choice-free ✔

---

## 1. 各項の本物性判定（監査者が Lean 定義を精読）

### (A) res₁ 全射性 `cr39_surjective`（`CyclotomicSurj39.lean`）— **本物**
`∀ τ ∈ Gal(ℚ(ζ₃)/ℚ), ∃ σ ∈ Gal(ℚ(ζ₉)/ℚ), res₁(σ)=τ`。
`cg3_galois_order_two` の二分（τ=id → σ=id・`Hom.map_one`／τ=g=⟨cg3Conj,…⟩ →
σ=σ₂=`cg9Aut 2 5`）で閉じる。σ₂ の指標 `cs39_char_sigma2 = 2`（`cg9_subst_zeta 2`＋
`cm9_powers_distinct` 対偶・choice 不要）→ `2 % 3 ≠ 1` の if_neg で res₁(σ₂)=cg3Conj。
`cs39_conj_is_g` で非自明元 g=⟨cg3Conj,…⟩ を同定（cg3Conj_ne_id で id 枝を潰す）。
仮説 0 本。**監査残欠 (ii)「res 全射性未証明」を discharge**。空虚でない
（σ₂ は cg9Aut 2 5 の実自己同型）。

### (B) ∀n Φ_{3ⁿ} 既約性 `eitPhi_irreducible` （`EisensteinTowerInput.lean`）— **本物・n=2 hardcode でない**
`eitPhi_irreducible (n)(hn:1≤n) : pibIrreducible ratRing (ctsPhi n)`。
`n=m+1` に還元し `eitPhi_irreducible_succ m`。核は **`eitFreshman : ∀ m,
eitCong3 ((x+1)^{3^m}) (X^{3^m}+1)`＝freshman's dream の立方帰納**（`eitCong3_cube`：
(Q+3D)³ 展開を `eit_cube`（二項定理・係数 1,3,3,1）で得る・m 帰納・単項式立方
`eit_single_cube` で X^{3^{m+1}} を出し `eitCong3_trans`）。これを `eis_irreducible`
（Eisenstein 判定器・f 一般・完成済）＋`est_transport`（シフト輸送）へ投入。
**任意 n に閉じており n=2 hardcode / honest 仮説パラメータ無し**を精読で確認。
帰結 `cteField n hn` は任意 n で実 IUTField（下記 (F)）。

### (C) 一般代入自己同型 `csaAut`（`CyclotomicSubAut.lean`）— **本物の体自己同型（非空虚）**
`σ_a : ζ_n ↦ ζ_n^a`（`evalSum` で定義・商をくぐらず全域）。
- **map_mul `csaSub_mul` は fudge でない**: σ_a(y·z) で積 y·z（各 deg<2·3^{n−1}）の
  Φ_{3ⁿ} 剰余簡約が生む差は h·Φ_{3ⁿ}（`gnfCong_red`）で、これが ζ_n^a で
  評価すると 0（`csa_eval_hphi`←`csa_phi_at_pow_zero`：核恒等式 `cts_pow_sub_one`
  ＋体の整域性）。**設計 発見B の「一般 n では余因子が非定数」を短絡なしで正面から
  解消**。（CE39 の nf=2 定数余因子短絡に依存していない。）
- **逆元 witness 非退化**: `left_inv`/`right_inv`＝`csaSub_left_inv`（a·a'≡1 mod 3ⁿ を
  生成元一点 ζ_n^{a·a'}=ζ_n^1=ζ_n に還元・`cae_endo_ext`）。invFun=csaSub a' は
  実写像で `inv0=0` 型縮退でない。
- `csaAut_mem`（ℚ 各点固定）・`csaAut_zeta`（σ_a(ζ_n)=ζ_n^a）確立。

### (D) 一般制限準同型 `ctrResHom` ＋ compat（`CyclotomicResTower.lean`）— **本物の群準同型・compat 全点**
`res_n : Gal(ℚ(ζ_{3^{n+1}})/ℚ) → Gal(ℚ(ζ_{3ⁿ})/ℚ)`、`res_n(σ) := σ_{a mod 3ⁿ}`
（a=`ctrChar σ`）。
- **map_mul `ctr_res_comp`**: `cae_aut_ext`（生成元一致）＋`cteMap_inj`＋compat 3 連鎖で
  `res(σ∘τ)=res(σ)∘res(τ)`。`ctrResHom` は `Hom`（map_mul 付き実群準同型）。
- **compat `ctr_compat`**: `ι_n ∘ res_n(σ) = σ ∘ ι_n` を全点 x で証明（段間決定補題
  `ctr_map_ext`＋生成元 `ctr_halpha`＋周期補題 `ctr_pow_period`）。生成元だけでなく
  分解 `cae_decompose` 経由で ∀x に拡張。

### (E) 一般全射性 `ctr_surjective`（`CyclotomicResTower.lean`）— **本物・空虚でない・choice-free 逆元**
`∀ n (hn:1≤n), ∀ τ ∈ Gal(ℚ(ζ_{3ⁿ})/ℚ), ∃ σ, res_n(σ)=τ`。
τ の指標 a=`ctr_charG τ`（a<3ⁿ・3∤a・`ctr_charG_nd3`）を 3^{n+1} 上へ持ち上げ、
**原像の逆元 witness を `ctr_inv_exists`（Hensel 持ち上げ）で choice-free に構成**:
m 帰納で mod 3 の逆元（`ctr_tex`・明示 t）を 3^{m+1}→3^{m+2} へ持ち上げる純 Nat 論法。
σ:=`csaAut (n+1) a ainv …`、`ctrChar σ = a`（冪相異性）・`a%3ⁿ=a` より
res_n(σ)=σ_a=τ（`cae_aut_ext` 生成元一致）。仮説 0 本。
**非空虚性**: `cteField 2 = p9iPhi9Field`（`cteField_two_eq`）で n=1 実例が実 ℚ(ζ_9)/
Gal に降りる。一般 n も cteField/csaAut の実インスタンスで witness が構成される。

### (F) ∀n 実体 `cteField n hn`（`CyclotomicEmbedTower.lean`）— **本物・任意 n**
`gefNFIUTField (ctsPhi n) (2·3^{n−1}) … (eitPhi_irreducible n hn)`＝A1 の一般体
エンジン（全域 inv 付き実 IUTField）を Φ_{3ⁿ} で実例化。`cteMap_mul`（★発見B）は
`cts_cong`＋`ctsStretch_mul`＋`pfdRed_char` で閉じ nf=2 短絡に非依存。`cteIota` 実 RingHom。
`cteMap_inj` は `ctsStretch_mul3` で係数復元＝本物の単射。

### (G) 補助資産の本物性
- `ctm_order`（位数ちょうど 3ⁿ・両向き）・`ctm_powers_distinct`（∀n）— telescoping 実証明。
- `ctmFind`（choice-free 走査）・`ctmFind_spec` — 有界 Nat 再帰、choice 皆無。
- `cae_aut_ext`（∀n 決定補題）— 基底分解 `cae_decompose`（y=Σ ι(y_i)·x̄^i）＋
  RingHom 保存＝本物。
- `cts_pow_sub_one`（X^{3ⁿ}−1 = Φ_{3ⁿ}·(X^{3^{n−1}}−1)）— stretch 帰納・本物。
- `cg9_galois_order_six` — 6 元の網羅リスト＋`prcDistinct`（相異）＋∀g∈L（悉皆）＝
  honest 仮説でない真の位数決定。

---

## 2. 正直な限定の妥当性（過大主張がないか・grep で裏取り）

ヘッダ記載の限定はいずれも**正しく**、過大主張は認められない。監査で裏取りした核心:

- **逆極限 profinite G_K は未構成（0.65 上限の主因）**: `ctl`/M3（逆極限オブジェクト）
  ファイルは **IUT/ に存在しない**（`ls IUT/ | grep -iE "ctl|towerlimit|inverselim"` 空）。
  M2 各ファイル（ResTower/SubAut/EmbedTower/MuTower/AutExt/Surj39/Stretch/Gal9）内の
  `ProfinitePi1Tower`/`InverseSystem`/`limitGrp`/`restr` の言及は
  **`CyclotomicResTower.lean` の限定コメント 2 行のみ**（grep 実行済）で、`ctrResHom` は
  `ProfinitePi1Tower.restr` witness／`InverseSystem.limitGrp` へ**未接続**。
  → A3 の本丸「有限 Galois 群の逆極限＝実 G_K」オブジェクトには**まだ到達していない**。
  一般機構（各段の有限 Gal 群＋全遷移射 res_n＋compat＋全射性）は揃ったが、
  **逆極限の組み立て・profinite 位相は未実施**。
- **群同型 Gal(ℚ(ζ_{3ⁿ})/ℚ) ≅ (ℤ/3ⁿ)^× は未証明（M4 未達）**: CG9 は位数 6 の元列挙、
  csa は σ_a 族の構成、ctr は res_n 全射性まで。**核が位数 3^{n−1} 巡回である等の
  完全な短完全列同型・群同型は未形式化**。
- p=3・ℚ 固定・分離性/正規性の一般論も未形式化。

これらの限定は各ファイルヘッダに明記され、削除・弱化されていない（§4 規約遵守）。

---

## 3. 判定の根拠（0.65 が妥当・0.7 を超えない・据え置きでない）

前回 0.6 は「2 段塔＋遷移射 res₁ 1 本（存在・compat・群準同型のみ・全射性なし・
一般 n なし）」。今回、**質的に別段階**へ進んだ:
1. res₁ 全射性 discharge（監査残欠 (ii) 解消）。
2. 2 段固定 → **∀n の全機構**（一般体 cteField・一般段間 ι cteIota・∀n 既約性・
   一般 μ ctm・一般決定補題 cae・**一般体自己同型 σ_a**・**一般群準同型 res_n＋compat
   ＋全射性**）。これは有限 Gal 群の cofinal 有向系（全遷移射が本物・全射・compat）を
   丸ごと構成したことに相当し、逆極限オブジェクト組み立てと profinite 位相を残すのみ。

status スケール（0.5=忠実な部分ケース／0.7=部分ケースを質的に超えるが満点未満）に照らし、
「単一遷移射」から「一般 n の全遷移系」への飛躍は 0.6 を明確に上回る。設計 §5 M2 予測 0.65 と一致。
一方 **A3 の literal target「逆極限」オブジェクトが未組み立て・M4 群同型も未達**ゆえ
**0.7 には届かない**。過大評価を避け **0.65** を確定。0.63 等の更に保守的値も検討したが、
入った本物の量（∀n 一般機構全部＋全射性）と設計予測一致から 0.65 が適正で、
過小でもある（据え置き 0.6 は今回の質的飛躍を反映せず不正直）。

## 4. complete_pct への反映・丸めの正直な扱い

- ledger: A3 status 0.6 → **0.65**（weight 12 不変）。
- `python3 tools/compute_complete_pct.py` → **A: 38 → 39**（Σ(w·s)/Σw: 38.0→38.6・round 38.6=39）。
  **今回は丸め閾を越えて柱A% が +1（38→39）動く**（前回 A3 ラウンドは 37.4→38.0 で 37→38、
  その前は 36.8→37.4 で 37 据え置きだった）。
- graph-meta.json: `pillars.A.complete_pct` 38→39・`complete_note`／`_last_round` を
  ledger 由来値と厳密一致で更新（フック `complete_pct_guard.sh` の graph-meta=台帳一致を満たす）。
- graph.json 再生成（`tools/gen_graph.py`・modules=590 edges=1114）。graph.json は
  構造データのみで pct を埋め込まない。
- dashboard.md 二軸表 A 行 38%→39%・5 柱平均 A38→A39（総合 ≈32%＝158/5・保守表記）を同期。
- 他柱（B18/C41/D18/E42）不変。

コード（IUT/*.lean）は不変更。共有ファイルは監査確定分（ledger/graph-meta/graph.json/
dashboard/本監査 doc）のみ更新。
